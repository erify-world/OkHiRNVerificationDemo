name: Auto Tag\n\non:  \n  push:  \n    branches: [ main ]\n\njobs:  \n  build:  \n    runs-on: ubuntu-latest\n\n    steps:  \n    - name: Checkout code  \n      uses: actions/checkout@v2\n\n    - name: Set up Node.js  \n      uses: actions/setup-node@v2  \n      with:  \n        node-version: '14'  \n\n    - name: Install dependencies  \n      run: npm install  \n\n    - name: Run tests  \n      run: npm test  \n\n    - name: Create Tag  \n      id: tag  \n      run: |  \n        if [ "${{ github.event.head_commit.id }}" ]; then  \n          echo "::set-output name=tag::v1.0.0"  \n        fi  \n\n    - name: Push tag  \n      if: steps.tag.outputs.tag  \n      run: |  \n        git tag ${{ steps.tag.outputs.tag }}  \n        git push origin ${{ steps.tag.outputs.tag }}  \n\n    - name: Optional Changelog  \n      run: |  \n        # Add logic for generating changelog here if necessary  \n        echo "Changelog generation skipped."